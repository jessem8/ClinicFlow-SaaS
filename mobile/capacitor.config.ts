import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clinicflow.mobile',
  appName: 'ClinicFlow',
  webDir: 'dist',
  backgroundColor: '#030712'
};

export default config;
